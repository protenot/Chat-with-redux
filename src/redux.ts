/* import {createStore} from "redux";
import {State, Action} from "./types"
function reducer(
    state:State={name:"",messages:[]},
    action:Action

): /* State
{
    switch(action.type){
        case
    }
} */
